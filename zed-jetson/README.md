# zed-ros2-wrapper on Jetson

Currently WIP. For running Stereolabs' `zed-ros2-wrapper` on ROS2 Jazzy (which is pinned to `24.04`), in Docker, on JetPack 6.2.2 (which is `22.04`-based), on the Nvidia Jetson Orin Nano.